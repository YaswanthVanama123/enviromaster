# ============================================================================
# EnviroMaster Frontend - Environment Variables Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to version control!
# ============================================================================

# ============================================================================
# API CONFIGURATION
# ============================================================================
# Backend API Base URL
# For local development: http://localhost:5000
# For production: https://your-backend-app.onrender.com (without trailing slash)
VITE_API_BASE_URL=http://localhost:5000

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
# 1. For local development:
#    - Use: http://localhost:5000
#    - Make sure backend is running on port 5000
#
# 2. For production deployment on Render:
#    - Set VITE_API_BASE_URL to your deployed backend URL
#    - Example: https://enviromaster-backend.onrender.com
#    - NO trailing slash!
#    - Must match the ALLOWED_ORIGINS setting in backend .env
#
# 3. Building for production:
#    - Run: npm run build
#    - The environment variable will be baked into the build
#    - Dist folder will contain the production-ready files
#
# ============================================================================
